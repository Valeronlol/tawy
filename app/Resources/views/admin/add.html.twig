{% extends 'admin/admin.html.twig' %}

{% block content %}
    {#<h1 style="text-align: center; color: red;">ADMIN body: {{ test }}, {{ test2 }}</h1>#}
    <h1>Add PAGE</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
